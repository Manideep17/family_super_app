// Re-export for any legacy imports — the canonical [FamilyMember] now lives
// alongside the family feature where each family carries its own roster.
export '../../../family/domain/entities/family_member.dart';
export 'family_role.dart';
