# Copy to android/key.properties and fill in (file is gitignored).
# Paths are relative to the android/app/ directory.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=upload-keystore.jks
